name: masrofy

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_auth: ^6.4.0
  firebase_core: ^4.6.0
  cloud_firestore: ^6.2.0
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assetes/images/image.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assetes/images/image.png"

flutter:
  uses-material-design: true
